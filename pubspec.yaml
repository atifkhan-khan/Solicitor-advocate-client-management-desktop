name: flutter_desktop_app_advocate
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  intrinsic_grid_view: ^0.0.2
  intl: ^0.19.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  window_manager: ^0.3.8

dev_dependencies:
  build_runner: ^2.4.5
  hive_generator: ^2.0.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

